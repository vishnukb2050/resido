export const getThemeColors = (tenantId?: string) => {
    // Pure Black & Radial Blue combo for all pages/communities
    return {
        primary: '#2563eb', // Radial Blue
        background: '#000000', // Pure Black
        surface: '#0d0d0d', // Pure Black Surface
        accent: '#60a5fa' // Light Radial Blue Accent
    };
};
