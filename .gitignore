# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm
.eslintcache

# Build outputs
dist/
build/
.next/
out/

# Environment variables
.env
.env.local
.env.production
.env.development
!.env.example

# Secrets / private keys (never commit — rotate immediately if one leaks)
*.pem
*.p8
*.pfx
*.cer
resido.pem

# OS files
.DS_Store
Thumbs.db

# IDEs
.vscode/
.idea/
*.swp
*.swo

# Prisma
/prisma/migrations/
/prisma/generated/
schema.prisma.backup

# Docker
.dockerignore

# Mobile (React Native / Expo)
.expo/
android/app/build/
ios/build/
*.jks
*.p12
*.key
*.pem
*.mobileprovision

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Temporal
.tmp/
temp/

# Terraform (never commit — run `terraform init` locally / in CI)
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraform.tfstate.lock.info
